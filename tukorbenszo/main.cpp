#include <iostream>
#include <string>

using namespace std;

int main()
{
    int sz=0;
    string szoveg, megoldas;
    getline(cin, szoveg);
    bool kiir=true;
    for(int i=0; i<szoveg.size(); i++){
        while(){
            b=x%10;
            x=x/10;
            c=c*10+b;
        if(szoveg[i]==' '){

        }
        if(szoveg[i]==' '){
            kiir=true;
        }
        if(kiir){
            megoldas=megoldas+szoveg[i];
        }
    }
    szoveg=megoldas;
    cout << szoveg;
    //cout << "are o selectie reper de desene etamina prezenta";
    return 0;
}
